{
  "editor.tabSize": 2,
  // "editor.cursorStyle": "block", // or "block", "underline", etc.
  // "editor.cursorWidth": 6, // Adjust as desired
  "actionButtons": {
    "loadNpmCommands": false,
    "commands": [
      {
        "tooltip": "run",
        "name": "$(run)",
        "color": "green",
        "command": "npm --silent start"
      }
    ]
  }
}